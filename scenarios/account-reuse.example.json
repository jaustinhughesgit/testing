{
  "name": "Existing account session is reused",
  "steps": [
    {
      "action": "newGroup",
      "path": ["newUser", "newUser"],
      "body": {},
      "expect": {
        "ok": true,
        "response": {
          "existing": true,
          "entity": "${state.userId}",
          "file": "${state.subdomain}"
        }
      }
    }
  ]
}
